/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'th'],
        defaultLocale: 'en',
        localeDetection: false,
      },
};

export default nextConfig;
